BOT_TOKEN=
# Redis URL for persistent storage (optional — falls back to in-memory for dev/test).
# Set in production to persist Teams, Members, StandupRuns, and History across restarts.
# REDIS_URL=redis://localhost:6379
